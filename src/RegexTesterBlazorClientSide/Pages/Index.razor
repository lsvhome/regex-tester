@page "/"
@using System.Diagnostics
@using System.Collections.Generic
@using System
@using System.Text.RegularExpressions;


<h1>C# Regex Tester</h1>

@*<script type="text/javascript">

        function RegexOptionsCheckboxClickedJS(name, value) {
            debugger.log(mame);
            //Debug.WriteLine(name + "="+value);
        }

    </script>*@
    Text:
<br />
<textarea id="fdfgc" class="wide" @bind="@text" @oninput="@ReCalcText">

</textarea>
<br />

<button @onclick="@(()=>{model.RegexOptionsVisible=!model.RegexOptionsVisible;})">RegexOptions</button>
<br />

<ul style="display: @(model.RegexOptionsVisible ? "block":"none")">
    @foreach (var each in Enum.GetNames(typeof(RegexOptions)))
    {

        <li>
            <input id="regexoptions" type="checkbox" value="@each"
                   onchange=@(eventArgs => { RegexOptionsChanged(each, (bool)eventArgs.Value); }) /> <label for="regexoptions">@each</label>
            </li>
        }

    @*

        aaa(@each, eventArgs.Value);


    *@
</ul>
<input type="radio" id="pattern" name="regexsourceradio" checked="@(model.RegexSource == RegexSourceEnum.Pattern)" onchange="@(() => model.RegexSource = RegexSourceEnum.Pattern)" />
<label for="patterninput">Pattern:</label>
<br />
<input id="patterninput" type="text" class="wide" @bind="@model.Pattern" @oninput="@ReCalcPattern" disabled="@(model.RegexSource != RegexSourceEnum.Pattern)" />
<br />

<input disabled type="radio" id="code" name="regexsourceradio" checked="@(model.RegexSource == RegexSourceEnum.CsCode)" onchange="@(() => model.RegexSource = RegexSourceEnum.CsCode)" />
<label for="codeinput">C# Regex code for matching:</label>
<br />
<input id="codeinput" type="text" class="wide" @bind="@cscode" @oninput="@ReCalcCode" disabled="@(model.RegexSource != RegexSourceEnum.CsCode)" />
<br />

<input id="autoruncheckbox" type="checkbox" @bind="@model.Autorun" /> <label for="autoruncheckbox">AutoRun</label>
<br />
<button @onclick="@ReCalc">Run</button>
<br />


    Results:


<br />
@if (Matches != null)
{
    @Decor()
}
<br /><br /><br />


@code
    {
IndexModel model = new IndexModel();
//bool RegexOptionsVisible = false;
//    RegexOptions regexOptions = RegexOptions.None;
   // public int RegexSource { get; set; } = 1;
    //public bool autorun { get; set; } = true;
    //private string pattern { get; set; }
    public string cscode { get; set; }
    public string text { get; set; }
@*public string pattern1 { get; set; }*@
@*public string res { get; set; } = "";*@
    MatchCollection Matches { get; set; }

    protected override async Task OnInitAsync()
    {
    #if DEBUG
    model.Pattern = "2(?"+"<"+"xx"+">"+".?)";
@*pattern1 = "1";*@
    text = "dadf 3  2s fdsf 2a g";
    cscode = "System.Text.RegularExpressions.Regex regex = new System.Text.RegularExpressions.Regex(\u0040\"abcd.*\");";
    #endif
    }

    async Task RegexOptionsChanged(string optionName, bool optionValue)
    {
    //debugger.log(mame);
    Debug.WriteLine(optionName + "=" + optionValue);
    //Debug.WriteLine(value);
    RegexOptions x = (RegexOptions)Enum.Parse(typeof(RegexOptions), optionName);


    if (optionValue)
    {
    model.RegexOptions = model.RegexOptions | x;
    }
    else
    {
    model.RegexOptions = model.RegexOptions ^ x;
    }
    Debug.WriteLine($"model.RegexOptions={(int)model.RegexOptions}");
    if (model.Autorun) ReCalc();
    }

    async Task ReCalcCode(Microsoft.AspNetCore.Components.UIChangeEventArgs g)
    {
    }

    async Task ReCalcPattern(Microsoft.AspNetCore.Components.UIChangeEventArgs g)
    {
@*@((UIChangeEventArgs __e) => CurrentValue = __e.Value)*@
    //object h = g.Value;
    //string gds = (string)h;
    model.Pattern = (string)g.Value;
    //    Debug.WriteLine(model.Pattern);
    //Debug.WriteLine(g);

    //res = model.Pattern + model.Pattern1;
    if (model.Autorun) ReCalc();
    }




    async Task ReCalcText(Microsoft.AspNetCore.Components.UIChangeEventArgs g)
    {
    text = (string)g.Value;
    if (model.Autorun) ReCalc();
    }




    async Task ReCalc()
    {
    Debug.WriteLine("ReCalc");
    Matches = Regex.Matches(text, model.Pattern, model.RegexOptions);
    }

    MarkupString
    Decor()
    {
    return RegexHelper.Decor(text, model.Pattern, Matches);
    }

    }
